
  <section data-id="{{book.id}}">
<table class="table">
  <thead>
    <tr>
      <th scope="col">Title</th>
      <th scope="col">Author</th>
    </tr>
  </thead>
  <tbody>
    {{#each books as |book|}}
    <tr>
      <td>{{book.title}}</td>
      <td>{{book.author}}</td>
    </tr>
    {{/each}}
  </tbody>
</table>
    </section>
